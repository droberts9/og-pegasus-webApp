.row.channels
  p.channel__title.pb-sm.mb-lg.ml-md2.mr-md2 {{ ::ctrl.caption }}
  .col-xs-6.col-sm-4.col-md-3.channel.mb-lg(ng-repeat="item in ::ctrl.items")
    a.channel__image(ui-sref="home.serie({slug: item.slug})")
      img(class="img-responsive" ng-src="{{ ::item.defaultImage }}" alt="{{ ::item.title }}")
    a.channel__link.mt-sm.mb-sm(href="{{ctrl.item_url(item)}}") {{ ::item.title }}
    .channel__description
      p {{ ::item.description }}

